<?php

/**
 * ONE_PATH points to the parent folder of the one|content core folder
 */
define('ONE_APPLICATION_PATH', JPATH_SITE);

/**
 * ONE_URI points to the URL where ... uhm ... I am not sure
 */
define('ONE_URI', JURI::root());